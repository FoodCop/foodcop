# FUZO Next.js Migration — Status Report

_Last updated: 2026-07-16 — Food Card Creation (master "+" modal, 4 card-type studios, `food_cards` schema), `/dashboard` built (recipe/restaurant/video rails), recommendation engine given real inputs for the first time (`aggregateForUser()` → `profiles.food_dna` → the existing `get_recommended_recipes` RPC). Same-day follow-ups: (1) discovered the entire Create Card feature was rendering unstyled (Tailwind classNames, no Tailwind installed) and hand-ported it onto a new `_studio.scss` partial; (2) the recipe flavor-enrichment script finished its full run — all 1,251/1,251 curated recipes now have real Gemini-generated flavor vectors in both `curatedRecipes.json` and the live `public.recipes` table, verified directly against Postgres; (3) **Playwright installed and actually driven against the running app** — the styling fix, the Dashboard, and the full Create Card flow (all 4 families, through to a real DB write) are now visually verified for real, not just structurally checked. Closes Gotcha #13's remaining caveat. Found and documented one real bug along the way (Gotcha #14: anonymous sign-in disabled on local Supabase). Follow-up same day: explicitly re-verified the Dashboard's 3 rails on request and found the YouTube one was actually broken (undeployed Edge Function, 503) — fixed with a new `/api/youtube/[action]` Route Handler (same pattern as `/api/gemini`/`/api/places`), and corrected a stale claim that Scout's Places backend was also broken (it wasn't — dead code, cleaned up). Also documented Gotcha #15: `.env`/`.env.local` both define `GEMINI_API_KEY`/`SUPABASE_SERVICE_ROLE_KEY`, and only `.env.local`'s copies are ever live. Final same-day follow-up (Gotcha #16): on request, converted Create Card (all 4 families) from dark to light theme, swept `vh` → `dvh` app-wide for mobile viewport correctness, and fixed a real bug found via a phone screenshot — the sticky `SiteHeader` bleeding through above the Create Card modal (portaled the modal to `document.body` + raised its z-index above Bootstrap's sticky navbar) — all visually confirmed via Playwright on an emulated mobile viewport._

## What this project is

Rebuilding FUZO (currently a vanilla Vite/HTML multi-page app at `K:\H DRIVE\Quantum Climb\APPS\FUZO_V3`) as a Next.js app. Approach: translate the old app's real pages/content/logic into Next.js routes, and borrow individual **components** (not whole template pages) from a set of Envato Next.js/HTML templates, re-skinned onto **one single master stylesheet** rather than importing each template's own CSS bundle.

**Working directory:** `C:\Users\mmu\Projects\fuzo-next` — this is the live project. (It started out mirrored from `K:\...\FUZO_V3\fuzo-next` early on, but the K: drive turned out to be an exFAT external SSD that can't reliably run Next.js's dev/build process — see Gotchas below. We stopped syncing back to K: and C: is now the only copy that matters.)

**Run it:** `npm run dev` (already aliased to `next dev --webpack` in package.json — see Gotchas for why webpack, not Turbopack).

## Template sources being harvested

| Source | Used for |
|---|---|
| Foodix (`fuzo_Next/foodix-nextjs`) | Homepage hero/sections (`home1/*` components) — still on Foodix's own CSS, not yet re-skinned |
| Ekomart (`Fuzo_Next2/ekomart-nextjs-main.zip`) | Original inspiration for the header/nav (now rebuilt on plain Bootstrap navbar) |
| Romio (`Dating/Main File/.../Romio`) | Login/Onboarding form patterns, and the Discover swipe-card mechanic (`Home.tsx`'s Tinder-style drag physics) |
| Soziety (`Soziety/Mobile-Soziety-.../xhtml`) | Profile page layout (stat bar/tabs/follow), and the Trims vertical video reel (`reels.html`) |
| Old FUZO app (root `*.html` files at `K:\...\FUZO_V3`) | Source of truth for real content/behavior: `quiz.html`'s 5-module Food DNA quiz data, `onboarding.html`'s A/B/C branching flow, `dna.html`'s charts, `profile.html`'s structure, `curatedRecipes.json` (1,251 real recipes), `rewards.html`/`ai-chef.html` + their per-page CSS + `/public/SVG/` icon library (837 files) — this static prototype is a *second*, more visually-finished design reference alongside the legacy React app (`legacy/fuzoapp/`); check both when porting a feature. |

Note: Proty's `ScoutMap.tsx` (Google Map wrapper component) was tried early on for Scout but ended up unused scaffolding — Scout's actual map is hand-rolled directly against `window.google.maps` in `ScoutView.tsx`. The Proty file and its CSS module were deleted.

## Pages built so far

- **`/` (home)** — Foodix hero/homepage sections. Still using Foodix's own compiled CSS (lowest priority to re-skin; not broken).
- **`/scout`** — Full-bleed Google Map (`ScoutView.tsx`, native `google.maps` API, no wrapper library) with live Google Places "nearby restaurants" search, route planner, discovery panel (desktop sidebar + mobile bottom sheet), place detail modal, and an add-pin wizard. Styled on `_scout.scss` using FUZO's brand hues (chili/turmeric/lime/sky/rose semantically mapped to map-marker sources). **Correction to a stale claim in an earlier version of this report:** the "Places search backend 404s / falls back to a hardcoded list" gap this bullet used to describe **is not actually true** — verified directly this session (Playwright hit `POST /api/places/nearby` and got real NYC restaurants back, real ratings/distances). `PlacesService` never actually depended on the Supabase Edge Function this report blamed; it already goes through `/api/places/[action]/route.ts` (a real Next.js Route Handler, same pattern as `/api/gemini`). The `SUPABASE_URL`/`EDGE_URL` constants pointing at an undeployed `make-server-5976446e` function were dead code in `placesService.ts` (never referenced by any actual call) and have been removed. Not clear when this was fixed vs. just never actually broken as described — but it's real and working now, confirmed, not assumed. **This session (Create Card work):** `ScoutAddPinModal` (the add-pin wizard) was generalized in place rather than duplicated — it now takes a `cardType` prop, gained a Flavor/Tags step, and publishes through the new `foodCardService.createFoodCard()` instead of calling `ScoutPersistence.saveScoutFind()` directly (that call still happens internally, as a dual-write, so pins keep showing up on the map exactly as before). See Food Card Creation section below — this is the Restaurant family of that flow, not a separate feature.
- **`/dashboard`** (built this session) — Previously nav-linked but 404ing (no page existed). Now three real-data rails, all confirmed rendering real data via Playwright: **Recommended Recipes** (RPC-matched once the user has food_cards, cuisine/diet-overlap fallback against onboarding prefs before that), **Restaurants Near You** (browser geolocation → `PlacesService.searchNearby`, real and working — see the corrected `/scout` bullet above), and **Watch & Cook** (YouTube results via `YouTubeService.searchVideos`, keyed to the user's top cuisine, now routed through `/api/youtube/[action]` — see "Playwright verification pass" for why that route exists). See the new Recommendation Engine section below for how the matching actually works. `src/components/dashboard/DashboardView.tsx` + `src/app/(main)/dashboard/page.tsx`, styled on new `_dashboard.scss`.
- **`/login`** — Email/password + Google OAuth + guest (`signInAnonymously`) via Supabase, re-skinned onto Bootstrap forms. **Correction:** the guest path is currently broken against local Docker — see Gotcha #14 (anonymous sign-ins disabled by GoTrue default). Email/password sign-in/sign-up is real and working.
- **`/onboarding`** — Full PATH_SELECT → A (Discover Food)/B (Create & Inspire)/C (Grow My Food Business) branching wizard, ported verbatim from the old app's `onboarding.html` (not `quiz.html` — that was a correction mid-session). Re-skinned a second time this session onto a Claude Design mockup's look (soft rounded cards, pill chips/buttons, `_onboarding.scss`) using this app's real brand tokens/fonts instead of the mockup's generic oklch palette — and every emoji glyph (path/type-picker icons, quiz result icon) replaced with a real `/SVG/...` icon. Writes to `users`/`taste_profiles`/`user_stats` on finish — **now verified working end-to-end against the real local Supabase** (see Backend section below); previously these three tables didn't exist at all, so every onboarding save was silently failing.
- **`/profile`** — Soziety-derived layout: avatar/stats/follow/message, Bites/Activity/Food DNA tabs. The "Food DNA" tab has the full `dna.html` chart set ported: score bars, animated fingerprint radar, flavor bars, cuisine bars, stat counters, match card, badges. **This session:** wired real data in where onboarding actually collects it — `ProfilePage` fetches the signed-in user's `users`/`taste_profiles` rows and passes real cuisines/dietary/quiz-personality into `FoodDnaSection`; a real "Your Food Personality" card and dietary chip row now render from that. Everything else in Food DNA (exploration score, the 5 DNA axis bars, fingerprint, food stats counters, match card, badges) has no real data source yet (onboarding never collected it) and is now explicitly labeled "Demo preview" instead of silently posing as the user's real stats. The person/restaurant demo toggle still exists as a design-preview fallback for when there's no session.
- **`/discover`** — Three tabs (`DiscoverTabs.tsx`). **Feed**: Tinder-style swipe card feed (ported from Romio's `Home.tsx` drag mechanic) over real recipes from `curatedRecipes.json`. Liking is local-state only so far (no Supabase persistence yet). **Bites** (built this session): a recipe finder — search bar, dietary/cuisine filter chips, recipe grid with skeleton/empty states, pagination, and a detail modal (Ingredients/Steps/Nutrition tabs, local save toggle) — also sourced from `curatedRecipes.json`, styled on a new `_bites.scss`. This replaced an earlier half-finished port of the legacy app's much larger Bites Studio (AI photo→recipe generation, social-video "Trim" extraction, share-to-chat) that turned out to be styled entirely in Tailwind classNames with no Tailwind installed in this project — it rendered unstyled and was discarded rather than fixed in place; see Gotchas. **Trims**: reuses the same `TrimsReel` component as the standalone `/trims` page below.
- **`/trims`** — Vertical video reel (ported from Soziety's `reels.html`) using 5 real bundled video files as placeholder content. Like/share/more-options work locally; no persistence yet.
- **`/rewards`** — Level/XP ring, 6 role progression tracks (Explorer/Reviewer/Blogger/Travel/Recipe/Instructor) each with 6 badges, achievement milestone grid, and a local-only demo simulator to trigger XP/badges. Combined the legacy React business logic (unchanged gamification math) with real badge/achievement icons ported from `FUZO_V3/public/SVG/`. Styled flat/brand-accented (`_rewards.scss`), not FUZO_V3's claymorphism reference — that soft-shadow surface style was already dropped project-wide (see `_variables.scss`'s comment).
- **`/ai-chef`** ("Tako") — Combined the legacy React chat engine (structured Gemini JSON responses: bullets/selectable cards/action buttons/follow-up chips) with FUZO_V3's `tako.js` landing screen (time-of-day greeting, search bar, Food Mood picker, Quick Discovery grid, Quick Links). Every mood/quick-action does something real now (navigates to an existing route or starts a real chat), unlike `tako.js`'s toast-only stubs. Also available as a **global floating FAB** (`TakoWidget.tsx`, mounted in `(main)/layout.tsx`, hidden on `/ai-chef` itself to avoid duplicating the UI) that opens the same assistant in a full-screen overlay from any page. Light theme (`_tako.scss`), icons are real `/SVG/...` files (not emoji) for cross-device rendering consistency.

All five of `/`, `/scout`, `/discover`, `/trims`, `/rewards`, `/ai-chef` share one persistent layout (`src/app/(main)/layout.tsx`) that renders `SiteHeader` + `TakoWidget` once — this matters, see Gotchas. `/login` + `/onboarding` share `src/app/(auth)/layout.tsx`. `/profile` has its own `src/app/profile/layout.tsx`.

## Food Card Creation (new, cross-cutting — not a single page)

The client's ask: a new master "start" modal with ~12 card types across 4 families (Recipe, Restaurant, Video, Discovery), replacing the single generic "manual 4-step wizard" the reference docs (`docs/card_creation_analysis.md`, `docs/fuzo-recommendation-engine-solution.md`, `docs/fuzo-food-card-demo.html`) described. Built as:

- **`public.food_cards`** (new migration, `supabase/migrations/20260717000000_food_cards.sql`) — the canonical table every card type writes to: `card_type`, `title`, `caption`, `tags` (jsonb, categorized: cuisine/food_type/meal_type/cooking_style/diet/occasion/season/price_level), `flavor_profile` (jsonb, 10-axis, written for **every** type now, not just recipes — that was the exact gap `card_creation_analysis.md` flagged), plus type-gated `ingredients`/`nutrition` (recipe), `place_id`/`lat`/`lng` (restaurant), `media_url` (video). Same migration also defensively `CREATE TABLE IF NOT EXISTS`s `posts`/`fuzo_locations`/`saved_items`/`idempotency_keys` — code already writing to those (`ScoutPersistence`, `PlateService`, `IdempotencyService`) had no migration creating them at all. **This migration has not been applied/verified against the local Docker instance** (unlike the `users`/`taste_profiles` migration from the previous session, which was verified end-to-end) — do that before relying on any of this.
- **`src/lib/services/foodCardService.ts`** — single `createFoodCard()` write path used by every studio; type-gates fields, and for the Restaurant family also dual-writes to `fuzo_locations` via `ScoutPersistence.saveScoutFind` so cards keep appearing on the Scout map.
- **`src/components/create/CreateCardModal.tsx`** — the master type-picker (Step 0), routes to whichever studio owns the selected `card_type`.
- **Restaurant family** (`RESTAURANT_VISIT`/`CAFE_VISIT`/`STREET_FOOD`) — reuses `ScoutAddPinModal.tsx` in place (see `/scout` bullet above), not a duplicate.
- **Recipe family** (`RECIPE`/`HOME_COOKING`/`DESSERT`/`DRINK`) — new `RecipeCardStudio.tsx`: optional photo → title/cuisine → description + ingredient rows → Gemini neural-reveal pass → editable review (flavor sliders, nutrition, ingredients) → publish/draft.
- **Video family** (`BITE_VIDEO`) — new `VideoCardStudio.tsx`: upload video (thumbnail-frame extraction + Gemini analysis) or paste a YouTube URL (zero-LLM path — oEmbed-style metadata + local taxonomy keyword tagging via `YouTubeService.getVideoDetails`).
- **Discovery family** (`FOOD_REVIEW`/`FOOD_EXPLORATION`/`FOOD_RECOMMENDATION`/`FOOD_COLLECTION`) — new `DiscoveryCardStudio.tsx`, one component for all four (label copy only differs by type): optional photo → caption/rating → Gemini tag/flavor pass → review → publish/draft.
- Shared new primitives: `src/components/create/shared/FlavorSliders.tsx`, `TagChips.tsx` (categorized chip picker sourced from the existing `taxonomy.ts` UGC lists), `Dropzone.tsx` (real HTML5 drag-and-drop + click-to-browse upload, added same day on request — used by Recipe/Discovery's photo step and Video's upload step; confirmed end-to-end via a Playwright-simulated file drop, not just the `is-dragging` visual state), `src/lib/types/foodCard.ts` (the `FlavorVector`/`CardTags`/`TYPE_META` single source of truth), and `GeminiService.analyzeCard()` (new method alongside the existing `analyzeBite`, routed through the same `/api/gemini` route — no server-side changes needed).
- **Deliberate deviation from `fuzo-food-card-demo.html`/`card_creation_analysis.md`, worth recording so it doesn't read as an oversight later:** those two docs describe *one* generic 4-step wizard (Type → Details → Tags&Flavor → Preview) reused identically for all 12 types, with no photo upload in the manual path at all (photos only via a separate universal "Import from URL" flow this app never built). This implementation instead built 4 different family-specific multi-step studios with a photo/video upload step folded into each one — following the AI/Snap/Bites/Trims Studio Ready Reckoner docs' pattern instead. Both are legitimate readings of the brief; the master type-picker (12 types/4 families) and the `saveCard()` database fix came directly from `card_creation_analysis.md` either way. Restaurant-family cards also don't call Gemini at all (pure Google Places via the pre-existing `ScoutAddPinModal`), where `card_creation_analysis.md` §3.2 envisioned Gemini identifying a dish/restaurant from a menu photo — not built.
- Entry point: a "+" button in `SiteHeader` (desktop icon row + mobile drawer), gated on `user` being signed in.
- **Superseded from the old plan:** the earlier "Not started yet" bullet about porting legacy `analyzeSnap`/`analyzeScreenshot`/`analyzeTrim` as separate per-feature Gemini methods is no longer the direction — one `analyzeCard(cardType, ...)` method serves all non-restaurant families instead of three parallel copies.
- **Uploaded video has no real storage pipeline yet** — `VideoCardStudio` currently sets `media_url` to a local `URL.createObjectURL()` blob, which won't persist or play back for anyone else/after a refresh. Needs a Supabase Storage bucket + upload step (same pattern as legacy `snapPersistence.ts`'s image upload) before video cards are real.
- **Explicitly deferred** (per the original plan for this feature): `aggregateForUser()` and real flavor-vector matching. That's exactly what the Recommendation Engine section below now covers.

## Recommendation Engine (finally has real inputs)

`public.recipes.flavor_profile` and `public.profiles.food_dna` (jsonb columns) plus a working SQL function `public.get_recommended_recipes(p_user_id, p_limit, p_offset)` (real euclidean-distance flavor matching, random fallback when `food_dna` is empty) have existed since the very first migration — but **nothing ever wrote to either column**. `scripts/seed_recipes.ts` always uploaded `r.flavor_profile || {}`, and `curatedRecipes.json` (1,251 Spoonacular-derived recipes) never had real flavor-axis data to begin with, since Spoonacular doesn't provide it.

This session closed that gap instead of duplicating the RPC's matching logic in TypeScript:

- **`src/lib/services/recommendationService.ts`** → `aggregateForUser(userId)` computes a recency-weighted (90-day half-life decay) average `FlavorVector` from the user's own `food_cards`, plus cuisine/diet tag-frequency counts, and **writes the vector to `profiles.food_dna`** — the first real data that column has ever held. `getRecommendedRecipes()` then calls the existing `get_recommended_recipes` RPC directly once a user has ≥1 food card ("warm" path); for a brand-new user with zero cards ("cold start") it instead ranks `curatedRecipes.json` by cuisine/diet overlap against their onboarding `taste_profiles.cuisines`/`dietary` — real signal, not a fabricated flavor match. `getNearbyRestaurants()`/`getSuggestedVideos()` feed the dashboard's other two rails off the same aggregate's top cuisine.
- **`scripts/enrichRecipeFlavorProfiles.ts`** (new) — the piece that makes the *warm* path meaningful for the curated dataset: one Gemini call per recipe (title/cuisines/dishTypes/diets/ingredients as context) asking only for the 10 flavor axes, checkpointed back into both `curatedRecipes.json` and `public.recipes.flavor_profile`. **Now complete — verified, not just reported: all 1,251/1,251 recipes have a populated, non-zero `flavorProfile` in `curatedRecipes.json`, and all 1,251/1,251 rows in the live `public.recipes` table have a populated `flavor_profile` (checked directly against the local Docker Postgres via `docker exec ... psql`, not just the JSON checkpoint).** Concurrency was turned down to 1 and retries up to 10 mid-run (from the script's original 5/3) to work around Gemini rate-limiting on the full run. This means `get_recommended_recipes`'s "warm path" now has real flavor vectors on both sides of the match for every curated recipe, not just for users with food_cards — the recommendation engine is fully real end-to-end for the first time.
- No new tables were needed for any of this — `profiles.food_dna` and `recipes.flavor_profile` already existed and were simply never populated.
- **Deliberately not touched:** `dna.ts`'s 25-question 5-module quiz (adventure/luxury/comfort/social/health scores) — fully-built scoring logic, but no live route/UI ever calls it and `taste_profiles.dna_scores` is never written. Wiring that up is a separate, unstarted feature; the dashboard/recipe matching above is grounded entirely in the 10-axis `FlavorVector`, the one with a real working SQL consumer.

## Playwright verification pass (new)

No browser-automation tool was available for most of this session (see the old Gotcha #13 caveat), so `playwright` was added as a devDependency (`npm install -D playwright && npx playwright install chromium`) specifically to close that gap — actually driven against the live `npm run dev` server, not just typechecked.

Flow exercised: home → login → sign-up → `/dashboard` → open the "+" Create modal → screenshot the type-picker → open each of the 4 families (Recipe/Restaurant/Video/Discovery) → for Discovery, click all the way through Source → Story → Neural Reveal → Review (tags + flavor sliders) → Save Draft → Success screen. Also applied `20260717000000_food_cards.sql` to local Docker first (it was sitting unverified — see the migration bullet above) so the Discovery flow's Publish/Save Draft could be checked against a real database write, not just a UI state change.

**Results:**
- **Every screen renders correctly** — dashboard (recipe cards with real images, cold-start nudge, geolocation-denied fallback), the type-picker grid, all 4 studios' first steps, `NeuralReveal`'s animated reveal screen, `TagChips`'/`FlavorSliders`' full styled layout, and the lime success screen. Confirms Gotcha #13's fix is real, not just structurally plausible.
- **A real `food_cards` row was created end-to-end**: `docker exec ... psql` confirms a `FOOD_REVIEW` / `DRAFT` row landed with the expected shape. Its `flavor_profile` came back as exactly `DEFAULT_FLAVOR` because the Gemini analysis call hit a 429 (rate-limited, likely residual load from the earlier bulk recipe-enrichment run) — and the graceful-fallback path in `DiscoveryCardStudio.runAnalysis()`'s catch block worked exactly as designed: it logged a warning and kept the default vector instead of crashing the flow. Good sign, not a bug.
- **One real bug found and documented (not fixed — out of scope for a verification pass):** see Gotcha #14. `signInAnonymously()` ("Continue as Guest" on `/login`) 422s against this local Supabase instance because anonymous sign-ins aren't enabled. The test switched to real email/password sign-up instead, which worked immediately (local GoTrue auto-confirms, no email-verification step needed).
- Other console output during the run (hydration mismatch on Tako's search input `caret-color` on the homepage, a Swiper "not enough slides for loop mode" warning, a few SVG width/height aspect-ratio warnings) — pre-existing/already-known-limitation noise, not new regressions from this session's work.
- Ad-hoc test scripts (`.pw-test.js` etc.) were scratch files, deleted after use — no permanent test suite exists yet. Worth setting one up properly (`@playwright/test`, a `tests/` directory, an npm script) if this kind of verification should happen regularly rather than ad hoc.

**Follow-up same session: dashboard's third rail was actually broken, found by explicitly re-verifying all three.** The first pass above confirmed the Create Card styling fix but didn't scrutinize whether all three Dashboard rails had *real* data behind them. Asked directly to confirm, and checked properly this time (granted geolocation in the Playwright context, inspected the actual DOM node counts per section, logged the relevant network calls):
- **Recipes: real** — 8 real recipe cards.
- **Restaurants: real** — 6 real NYC places via `POST /api/places/nearby`, real ratings/distances.
- **Videos: were completely broken** — `GET .../functions/v1/youtube-proxy?...` returned `503`, because that Supabase Edge Function was never deployed to this project (exactly the risk flagged when the Dashboard was first built, and exactly the same "no browser check" failure mode as Gotcha #13, just for a different feature). The section didn't show an error - it just silently rendered nothing, so it was easy to miss without deliberately checking network calls, not just screenshots.
- **Fixed properly, not patched around:** added `src/app/api/youtube/[action]/route.ts` — a direct Next.js Route Handler using `YOUTUBE_API_KEY` (already sitting unused in `.env`), same pattern as `/api/gemini` and `/api/places/[action]`. Rewrote `src/lib/services/youtubeService.ts` to call it instead of the Supabase Edge Function (dropped the now-irrelevant `SUPABASE_URL`/`SUPABASE_ANON_KEY`/auth-header plumbing entirely, not just re-pointed the URL). Re-verified after the fix: `GET /api/youtube/search?...` now returns real `200`s with real YouTube results, and all 8 video cards render with real thumbnails/titles/channels. Screenshotted, not just asserted.

## Backend: new working Gemini route

`src/app/api/gemini/route.ts` is a real, working Next.js Route Handler that calls the Gemini API directly server-side using `GEMINI_API_KEY` (already present in `.env`, never exposed to the client). This replaces both legacy `GeminiService` copies' local-Vite-proxy → Supabase-edge-function fallback pattern, neither of which works in this app. `src/lib/services/geminiService.ts` (client) talks to `/api/gemini` only. This is the first real backend integration in the Next.js port — worth reusing this pattern (direct Route Handler, not a Supabase Edge Function) for other AI-backed features instead of assuming an edge function exists.

## Backend: Supabase schema now matches the app code (local Docker)

A **new local Supabase project is now running in Docker** (`supabase start`, no `config.toml` in the repo but the CLI still resolves it via `.temp/linked-project.json` / `.branches/_current_branch` — see Gotchas). `.env.local` points at it (`NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321`).

This session found and fixed a real, silent bug: `supabase/schema.sql` / the one migration file only ever created `public.profiles`/`public.recipes`/`public.user_interactions`, but the actual app code (login, the OAuth callback, onboarding's `finalize()`, `chatService.ts`) was written against `public.users`/`public.taste_profiles`/`public.user_stats` — none of which existed. Every one of those Supabase calls was failing silently (none of the call sites check the returned `error`), so registration wasn't persisting an app-level user row and onboarding answers were going nowhere, while the UI behaved as if everything succeeded.

Fixed with `supabase/migrations/20260715020000_app_users_taste_profiles_stats.sql`: adds `users` (display_name/username/avatar_url/bio/location/website/phone/profile_type/profile_subtype/is_onboarded/is_online/last_seen/is_master_bot/points_total), `taste_profiles` (cuisines/dietary/result_emoji/result_title/result_desc), and `user_stats`, with RLS policies + grants matching the existing tables' pattern, plus an extended `handle_new_user()` trigger that now seeds `public.users` on **every** `auth.users` insert — including the anonymous "Continue as Guest" sign-in path, which never called the client-side upsert in `login/page.tsx`.

Verified for real, not just statically: signed up a real test user via the Auth REST API, confirmed the trigger seeded `public.users`, replayed onboarding's exact `finalize()` writes under real RLS as that authenticated user (all three tables), then confirmed cascade-delete cleanup. Also replayed the exact `ProfilePage` read queries and got the written data back.

**Still open:** this is only applied to the local Docker instance (applied directly via `docker exec ... psql`, not through `supabase migration up`/`db push` — see Gotchas for why). Whichever Supabase project ends up hosting this app for real still needs this migration (and the `scripts/seed_recipes.ts` recipe seed, if that route is ever preferred over `curatedRecipes.json`) applied to it too.

## Styling architecture (the big consolidation)

One master stylesheet: `src/scss/main.scss` → `_variables.scss` (FUZO brand colors: chili/turmeric/lime/sky/rose as Bootstrap `$primary`/etc. overrides) → real `bootstrap/scss/bootstrap` → feature partials (`_header.scss`, `_scout.scss`, `_auth.scss`, `_profile.scss`, `_dna.scss`, `_feed.scss`, `_trims.scss`, `_rewards.scss`, `_tako.scss`, `_onboarding.scss`, `_bites.scss`, `_dashboard.scss`, `_studio.scss`), imported once in the root layout. (Create Card briefly regressed this rule with raw Tailwind classNames — fixed same session, see Gotcha #13.) **Do not** import a template's own compiled CSS bundle again — hand-port the specific rules a component needs into a partial instead. This replaced an earlier (bad) approach of importing each template's whole CSS file per route, which caused width inconsistencies and eventually broke the Profile page outright.

**This app has no Tailwind, on purpose.** Ported components sometimes carry over Tailwind className strings from the legacy React app (which used Tailwind) — those are dead weight here and render unstyled. Don't install Tailwind to fix it: Bootstrap and Tailwind both ship utility classes with identical names but different values (`.mt-3`, `.p-4`, `.gap-2`, etc.), so adding Tailwind risks silently corrupting spacing across the *entire* app depending on CSS load order. Hand-port Tailwind classNames to a feature SCSS partial instead (see `_scout.scss`/`_rewards.scss`/`_tako.scss` for the pattern).

**Icon strategy:** `FUZO_V3/public/SVG/` has an 837-file icon library organized by category (`restaurant/`, `food/`, `foodies/`, `social/`, `map/`, `UI/`, `flags/`, ...). Don't bulk-copy it — copy only the specific icons a feature actually references into `fuzo-next/public/SVG/`, preserving the folder structure (Rewards copied 30, Tako copied 8 more, Onboarding's card-picker/result icons this session). These SVGs have **hardcoded fill colors baked in, not `currentColor`** — CSS `color`/`fill` inheritance tricks don't tint them. To render one as a clean white silhouette on a colored badge, use `filter: brightness(0) invert(1)` (see Tako's Quick Discovery/Quick Links cards); otherwise they just render in their own native colors, which is fine for badge/mood icons sitting on a neutral surface.

**For generic UI icons (search, clock, close, filter sliders, chevrons, tabs)** — as opposed to food/cuisine/badge imagery — use `lucide-react`. It's a real, already-used dependency (Chat, Scout, Rewards, Tako, and now Bites all import from it directly), not something to add. The `/SVG/` library doesn't have this kind of generic icon anyway (it's all food/restaurant/social-themed).

## Known gotchas (read before touching build config again)

1. **Turbopack can't compile our Bootstrap SCSS on this machine** — `@import "mixins/banner"` inside `node_modules/bootstrap/scss/bootstrap.scss` fails to resolve under Next 16's default Turbopack dev/build, but works fine under webpack. That's why `package.json` scripts force `--webpack`. Don't remove that flag without re-testing.
2. **`wowjs` was removed entirely.** It's a legacy UMD package with fragile bundler interop (broke differently under Turbopack vs webpack), and its required `animate.css` companion was never even wired in, so it wasn't producing any visible effect anyway.
3. **Bootstrap's JS must be loaded as a plain `<script>` tag, not a JS import.** A dynamic `import()` silently failed to build as a webpack chunk (`ChunkLoadError` timeout); a static import broke SSR (`document is not defined`, since Bootstrap's UMD touches `document` at eval time). Fix: `bootstrap.bundle.min.js` is copied into `public/vendor/` and loaded via `<Script src="/vendor/bootstrap.bundle.min.js" strategy="afterInteractive">` in the root layout — exactly how the original templates loaded it.
4. **Never use Bootstrap's JS-driven offcanvas for anything with a nav Link inside it.** Bootstrap's offcanvas appends a full-viewport backdrop div directly to `<body>`, outside React's tree. If a link inside it triggers Next.js client-side navigation while the close transition is still running, the backdrop can be orphaned — invisible, but it permanently blocks every click on every subsequent page (no console error, easy to mistake for "navigation is broken"). Fix applied: `SiteHeader`'s drawer + dropdowns are now 100% React `useState`-controlled, no `data-bs-toggle`/`data-bs-dismiss` at all. If you add another offcanvas/modal with links inside, do the same.
5. **`SiteHeader` must live in a shared layout, not be rendered per-page.** Re-mounting it on every navigation was part of the above race condition. `(main)/layout.tsx` renders it once. `TakoWidget` follows the same rule for the same reason.
6. **K: drive (exFAT) cannot run Next.js dev/build reliably** — concurrent directory operations Next.js relies on fail with EPERM/access-denied on that filesystem. Everything happens on C: now.
7. **`.env.local` has `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` reused from the old app** for now — fine for dev, rotate before anything resembling production.
8. **Flexbox/Grid mobile overflow footgun:** a flex item defaults to `min-width: auto`, so a horizontally-scrolling row nested inside a flex column (chips, pills, card carousels) can force its ancestor wider than the viewport instead of scrolling internally — and if an ancestor has `overflow: hidden` (common for full-bleed shells), the excess just gets silently clipped rather than showing a scrollbar. Fix: `min-width: 0` on the ancestor flex item. Same root cause with CSS Grid: bare `1fr` tracks size to content's min-content width, so long unbroken labels can blow out a column — use `minmax(0, 1fr)` instead. Hit and fixed on Tako; **not yet verified** on Rewards' achievement grid or Scout's wizard cuisine/photo grids, which use the same bare-`1fr` pattern.
9. **Prefer wrapping (`flex-wrap: wrap`) over horizontal scroll (`overflow-x: auto`) for chip/pill rows where content length is unbounded** — sidesteps gotcha #8 entirely instead of needing the `min-width: 0` fix. Used for Tako's Food Mood pills after the overflow bug above.
10. **A file existing under `src/` (even one that type-checks cleanly) doesn't mean it's a real implementation — check every "stub"-named file individually.** The pre-existing `src/components/bites/` port had both `feedStub.ts` (a genuine no-op — every method just returns success without doing anything) and `supabaseStub.ts` (misleadingly named — it actually re-exported the real, working Supabase client). Don't assume based on a filename; read each one. Also don't trust a clean `tsc --noEmit` as proof a ported feature works — the old Bites port type-checked perfectly while being 100% unstyled (see Tailwind gotcha above) and backed by fake Unsplash fallback data instead of this app's real `curatedRecipes.json`.
11. **The Supabase CLI in this repo has no `supabase/config.toml`**, yet `supabase status`/`migration list` still resolve a project ref from `.temp/linked-project.json`/`.branches/_current_branch` and talk to a **linked remote** project by default, not the local Docker instance — `supabase migration list` printed `"Connecting to remote database..."` even though the intent was to check local state. To apply a migration to the local Docker Postgres directly and unambiguously, use `docker exec -i supabase_db_<ref> psql -U postgres -d postgres < migration.sql` (container name from `docker ps`) rather than trusting `supabase db push`/`migration up` to target local by default.
12. **`/messages` and an `AuthProvider`/chat system (`src/components/chat/`, `src/types/{chatUi,auth,appItem}.ts`) now exist** but weren't touched or reviewed this session and aren't reflected elsewhere in this report yet — `messages/page.tsx` currently renders `ChatView` against hardcoded `MOCK_FRIENDS` data. Worth a proper status pass next session rather than assuming either "done" or "not started."
13. ~~The entire Create Card feature (this session) is unstyled~~ **Fixed, same session.** `src/components/ui/StudioStepper.tsx` and `NeuralReveal.tsx` were sitting in the repo pre-session already written in Tailwind utility classNames, and the rest of Create Card (`CreateCardModal.tsx`, `RecipeCardStudio.tsx`, `VideoCardStudio.tsx`, `DiscoveryCardStudio.tsx`, `FlavorSliders.tsx`, `TagChips.tsx`) was built the same way without checking whether Tailwind actually renders in this app (it doesn't — no `tailwindcss` in `package.json`, no config, no `@tailwind` directive anywhere). Exact repeat of gotcha #10's discarded first Bites Studio port, and it slipped through for the same reason: verification was `tsc`/`eslint`/`next build` only, never opened in a browser. Hand-ported onto a new `_studio.scss` partial, all 8 files converted to real BEM classes. Visually verified via Playwright screenshots, not just structurally. **Note: this was originally ported onto a dark theme** (mirroring `_scout.scss`'s `.scout-wizard`'s then-dark palette) — **both were converted to light mode in a follow-up same day**, see Gotcha #16.
14. **"Continue as Guest" on `/login` is broken against the local Supabase instance** — `supabase.auth.signInAnonymously()` returns a 422 (`"Anonymous sign-ins are disabled"`, confirmed via `docker logs supabase_auth_<ref>`). This repo has no `supabase/config.toml` (see gotcha #11), so anonymous sign-ins are just off by GoTrue's default rather than anything this app's code controls. Not fixed this session (found incidentally while setting up the Playwright pass above — switched to real email/password sign-up instead, which works fine and auto-confirms locally). Fix, if wanted: add a `supabase/config.toml` with `[auth] enable_anonymous_sign_ins = true` and restart the local stack — that's an infra change nobody asked for yet, so it's parked here rather than done.
15. **`.env` and `.env.local` both define `GEMINI_API_KEY` and `SUPABASE_SERVICE_ROLE_KEY`** — Next.js's env load order (`.env.local` overrides `.env`) means `.env`'s copies of those two are dead weight; only `.env.local`'s values are ever actually live. Found while adding the `/api/youtube` route, since `YOUTUBE_API_KEY` (the one key genuinely only defined in `.env`) is what made that fix possible. `.env` itself is mostly a carryover from the old Vite app (`VITE_`-prefixed vars Next.js never reads: `VITE_SUPABASE_URL`, `VITE_GOOGLE_MAPS_API_KEY`, etc. — the real ones are the `NEXT_PUBLIC_`-prefixed/unprefixed copies in `.env.local`), but it does supply real, actively-used server-side keys too (`YOUTUBE_API_KEY`, `SUPABASE_JWT_SECRET`, `SUPABASE_PROJECT_ID`, `FIGMA_ACCESS_TOKEN`, etc.) since Next loads both files. If you ever need to rotate or change `GEMINI_API_KEY`/`SUPABASE_SERVICE_ROLE_KEY`, edit `.env.local` — editing `.env`'s copy silently does nothing.
16. **Create Card was dark-themed, then converted to light mode + two mobile-fit bugs fixed, same day, on direct request** (screenshot showed the master type-picker on a real phone). Three real things came out of this pass, not just the requested theme swap:
    - **`vh` → `dvh` sweep, app-wide.** Every `Nvh`/`calc(100vh - Xpx)` in the SCSS (`_bites.scss`, `_feed.scss`, `_scout.scss` ×4, `_studio.scss`, `_trims.scss`) converted to `dvh`, since `vh` doesn't account for mobile browser chrome (address bar) showing/hiding, and can cause exactly the "not fitting on mobile" symptom that prompted this. `.studio-modal`/`.scout-wizard` also got an explicit `height: 100dvh` alongside their `inset: 0`.
    - **Real bug found and fixed: the sticky `SiteHeader` was bleeding through above the Create Card modal**, visible in the screenshot that prompted this whole pass. Root cause was two-layered: (1) `CreateCardModal` was rendered as a DOM child of `<nav className="sticky-top">` in `SiteHeader.tsx` — `position: fixed` nested inside a `position: sticky` ancestor is unreliable, particularly on mobile WebKit; fixed by portaling the modal to `document.body` via `createPortal` instead. (2) Even after portaling, `.studio-modal`'s z-index (200) was still lower than Bootstrap's `.sticky-top` (`$zindex-sticky: 1020` by default), so the header kept winning the paint order — fixed by raising `$studio-z`/`$scout-z-wizard` to 1100 (above Bootstrap's entire scale, max `$zindex-toast: 1090`). Confirmed fixed via Playwright: `modal.parentElement === document.body`, `modalRect.top === 0`, and a screenshot showing the header fully hidden behind the modal, not just "positioned right."
    - **Create Card (all 4 families, including the Restaurant/`ScoutAddPinModal` step) re-themed dark → light**, matching FUZO's established flat light look elsewhere (Scout's own non-wizard UI, Bites, Dashboard) rather than the dark theme it launched with a few hours earlier. `_studio.scss` and `.scout-wizard`'s section of `_scout.scss` both rewritten: white backgrounds, dark ink text, light gray surfaces/borders, turmeric accent kept for fills (badges, selected chips) but swapped for dark-ink fills on primary CTAs (better contrast on white than turmeric-as-text ever was). The map-scrim gradient in `ScoutAddPinModal`'s Pin step deliberately stayed dark — that's contrast styling for legibility over the actual (independently dark-styled) Google Map image, not page theme. Visually confirmed via Playwright on an emulated iPhone 13 viewport, both for the type-picker and for `ScoutAddPinModal`'s Search step.

## Not started yet

- ~~Create Card studios' real CSS landed but is browser-unverified~~ **Done** — see Gotcha #13 / "Playwright verification pass." Visually confirmed via screenshots, not just structurally.
- ~~`food_cards` migration is unverified~~ **Applied to local Docker and confirmed real end-to-end** — a Discovery-family card published through Playwright actually landed a row in `public.food_cards` with the expected shape. **Not yet specifically re-verified: the Restaurant family's `fuzo_locations` dual-write** (`foodCardService`'s restaurant branch) — the Playwright pass only opened `ScoutAddPinModal`'s first step, didn't publish a restaurant card all the way through.
- **Video cards have no real upload pipeline** — `VideoCardStudio` uses a local `URL.createObjectURL()` blob for `media_url`, not Supabase Storage. Not persisted/shareable yet.
- **Supabase is now real and working locally** (Docker, schema fixed — see Backend section), but only locally. Whichever Supabase project this app actually deploys against long-term (local Docker is presumably dev-only) still needs: both this session's and the previous session's migrations applied, and `scripts/seed_recipes.ts` (+ the flavor enrichment upserts) run if the `public.recipes` table route is ever preferred over the `curatedRecipes.json` static-file route. (No Edge Functions actually need redeploying for Places or YouTube — both go through this app's own Next.js Route Handlers now; see the corrected `/scout` bullet and the YouTube fix under "Playwright verification pass.")
- **Stub/out-of-scope pages** still 404: `/dna` (standalone), `/leaderboard`, `/settings`. (`/bites` was built earlier — it's the Discover tab, not a standalone route; `/dashboard` is built this session — see above; `/messages` has a real page, unaudited — see Gotcha #12. `/rewards` and `/ai-chef` are done — see above.)
- **Homepage** (`/`) still uses Foodix's own compiled CSS rather than the master stylesheet — lowest priority since it isn't broken, but it's the one remaining piece of "old" per-template CSS in the app.
- **`dna.ts`'s 25-question DNA quiz has no live UI** — scoring logic is fully ported and correct, but nothing calls it; `taste_profiles.dna_scores` is never written. Deliberately not wired into the Dashboard/recommendation work this session (see Recommendation Engine section) — needs its own decision: build the quiz UI, or retire the dead code.
- **Food DNA `/profile` tab is still mostly demo content** — exploration score, the 5 DNA axis bars, fingerprint, food stats, match card, and badges all need either real behavioral tracking (visits/reviews/likes - none of which exist as tables yet) or a deliberate decision to keep them decorative long-term. (Note: this is the *display* tab; the *matching* logic it would need is now real, per the Recommendation Engine section — it just isn't wired to this tab yet.)
- **User-generated recipes for Bites** — currently `curatedRecipes.json` only. Food Card Creation's Recipe family (`RECIPE`/`HOME_COOKING`/`DESSERT`/`DRINK`) is arguably this UGC path already, just landing in `food_cards` rather than being surfaced back into the Bites grid — worth deciding whether to merge them or keep the two surfaces separate.
- **"Continue as Guest" is broken on local Supabase** — see Gotcha #14. Anonymous sign-ins are disabled by GoTrue's default (no `config.toml` in this repo to override it). Low urgency for local dev (email/password sign-up works fine and was used instead), but would confuse anyone who hits it expecting the guest path to work.
- **No permanent test suite** — Playwright is now installed (`devDependencies`) and was proven out against the real app this session, but only as throwaway scratch scripts, deleted after use. Worth turning into a real `@playwright/test` suite if this level of verification should happen routinely rather than ad hoc.

## Suggested next steps

1. ~~Open the "+" Create modal in a real browser and eyeball every studio~~ **Done** — Playwright pass this session, see above.
2. Publish a Restaurant-family card all the way through and confirm the `fuzo_locations` dual-write still lands (the one Create Card path the Playwright pass didn't exercise to completion).
3. Decide on the long-term-hosted Supabase project (vs. local Docker) + apply both migrations + get credentials into whatever `.env` that deploy target uses. **Now also carries 1,251 freshly-enriched `flavor_profile` rows in local `public.recipes`** — re-running `scripts/seed_recipes.ts` against a new project would stomp them back to `{}` unless it's re-run *after* the enrichment script (or the enriched `curatedRecipes.json` is re-seeded as the source of truth, which already has the same data).
4. ~~Redeploy whichever Supabase Edge Functions are still needed~~ **Not actually needed** — Places and YouTube both go through this app's own `/api/places`/`/api/youtube` Route Handlers now, not Supabase Edge Functions. Double-check nothing else in the app still assumes an edge function exists before assuming this step is fully moot.
5. Build a real Storage-backed upload path for `VideoCardStudio` (currently a local blob URL).
6. Stub the remaining nav-linked pages (`/dna`, `/leaderboard`, `/settings`) so nothing 404s; do an honest status pass on `/messages`/chat/`AuthProvider` since that landed without a report update.
7. Verify the Grid/Flexbox mobile-overflow gotcha (#8 above) doesn't also affect Rewards' achievement grid, Scout's wizard grids, or the new Create Card type-picker grid.
8. Re-skin the homepage onto the master stylesheet (optional/low-priority cleanup).
9. Decide on `dna.ts`'s quiz UI (build it for real, or retire it) and on real Food DNA `/profile` data sources instead of leaving them permanently "Demo preview."
